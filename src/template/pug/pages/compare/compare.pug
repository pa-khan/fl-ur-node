extends ../../templates/default
block vars
  -
    title = "Сравнение"
    breadcrumbs = [
      ['Главная', 'index.html'],
      ['Профиль', 'profile.html'],
      [title]
    ]
block content
  include ../../../blocks/compare/compare
  +style('compare', 'blocks')
  +script('compare', 'blocks')
  
  - questionsClass = 'hidden-xs'
  include ../../../blocks/questions/questions
  +style('questions', 'blocks')